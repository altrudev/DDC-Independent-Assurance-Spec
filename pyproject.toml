[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ddc-independent-assurance-spec"
version = "0.1.0.dev0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
